apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: release-healthcheck
  labels:
    app: auth-service
data:
  DATABASE_HOST: "postgres"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "authdb"
  LOG_LEVEL: "info"
