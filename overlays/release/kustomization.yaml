apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - target:
      kind: ConfigMap
      name: auth-service-config
    patch: |
      - op: replace
        path: /data/DATABASE_HOST
        value: postgres-wrong
